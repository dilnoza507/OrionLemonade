import { ArrowLeftRight } from 'lucide-react';
import PageStub from '../components/shared/PageStub';

export default function TransfersPage() {
  return (
    <PageStub
      title="Межфилиальные трансферы"
      description="Перемещение сырья и готовой продукции между филиалами"
      icon={ArrowLeftRight}
    />
  );
}
